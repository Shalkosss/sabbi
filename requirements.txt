# Herramientas de build en Python. NO son dependencias de la aplicacion:
# la plataforma corre sobre Node. Esto es solo para regenerar la plantilla del
# PPT replica a partir del deck de referencia.
#
#   pip install -r requirements.txt
#   python packages/export/pptx/replica/tokenizar.py

python-pptx      # lectura y escritura del .pptx
openpyxl         # lectura de fichas y propuestas .xlsx
lxml             # manipulacion del XML de OOXML
oletools         # extraccion de las macros VBA de referencia
